# セキュリティガイドライン

本リポジトリおよびチームでの **秘密情報（APIキー・トークン・パスワード・接続文字列など）** の取り扱いルールです。過去に Reinfolib の APIキーがドキュメントへ直書きされ公開された事象を受けて整備しました。

---

## 1. 基本原則

- 秘密情報は **コード・ドキュメント・コミットログに絶対に書かない**。
- 実値は `.env`（`.gitignore` 済み）と本番のシークレット管理のみに置く。
- コード上は環境変数参照（例: `env('REINFOLIB_API_KEY')`）に統一し、値が断片に映り込まないようにする。
- `.env.example` には **ダミー値のみ**（例: `your-reinfolib-api-key-here`）。実値を書かない。

---

## 2. gitへ秘密情報をコミットしない仕組み

### 2-1. pre-commit フック（手元でブロック）

コミット前に [gitleaks](https://github.com/gitleaks/gitleaks) が秘密情報を自動検出します。各自の環境で一度だけ有効化してください。

```bash
# pre-commit 本体（未導入の場合）
pip install pre-commit        # または: brew install pre-commit
# リポジトリにフックを登録
cd /path/to/bird
pre-commit install
# 動作確認（任意）
pre-commit run --all-files
```

設定は `.pre-commit-config.yaml`、検出ルールの調整は `.gitleaks.toml` を参照。

### 2-2. GitHub 側の防御

- リポジトリ設定で **Secret scanning** と **Push protection** を有効化（Settings → Code security）。
- push/PR 時に `.github/workflows/secret-scan.yml` が gitleaks を実行し、すり抜けを検知する。

---

## 3. AIサービス利用ルール

生成AI（Claude / ChatGPT など）への問い合わせで秘密情報を漏らさないためのルールです。

- **実キー・`.env`・本番ログの生データを問い合わせに含めない。** 先頭数文字だけでも記載しない（完全に伏せる）。
- ログ・設定・コード断片を貼る前に、キー/トークン/パスワード/接続文字列を **プレースホルダに置換** する（例: `REINFOLIB_API_KEY=***REDACTED***`）。
- 迷ったら共有前にサニタイズ用チェック（後述のスキル、または `gitleaks detect` を貼り付けテキストに対して実行）を通す。

---

## 4. 漏洩時の対応フロー

1. **失効・再発行を最優先**：疑わしい時点で該当キーを提供元で失効させ、新キーを発行する。
2. 再発行依頼には **旧キーの先頭4桁** を記載（下記台帳から参照）。
3. **履歴の書き換え**：`git filter-repo`（または `filter-branch`）で全コミットからキーを除去し、`git push --force`。
4. 影響範囲を関係者へ **通知**。クローン済みの第三者には残る前提で対応する。
5. 台帳を更新（失効日・新キーの先頭4桁・ローテーション日）。

---

## 5. APIキー台帳テンプレート

キーの実値は記載しない。管理用メタ情報のみをパスワードマネージャ等の安全な場所で管理する。

| サービス名 | 先頭4桁 | 発行日 | 担当者 | 用途 | 最終ローテーション日 | 状態 |
|-----------|--------|--------|--------|------|-------------------|------|
| Reinfolib（不動産情報ライブラリ） | ____ | YYYY-MM-DD | | 物件データ取得 | YYYY-MM-DD | 有効 / 失効 |
| | | | | | | |

> 補足: 提供元によっては、迷惑メールフォルダ内の通知が一定期間後に自動削除される場合があります。重要通知は通常フォルダへ移動してください。
